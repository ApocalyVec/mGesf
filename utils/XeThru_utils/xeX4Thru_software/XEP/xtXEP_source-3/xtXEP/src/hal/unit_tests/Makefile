PRODUCT_NAME = HAL_test

HW_PLATFORM = X4M0x_SAMS70

BUILD_CONFIG = Debug

TOP_LEVEL_DIR = ../../..

TARGET_FOLDER = $(TOP_LEVEL_DIR)/build/$(PRODUCT_NAME)_$(HW_PLATFORM)/$(BUILD_CONFIG)

TARGETS = \
	$(TARGET_FOLDER)/$(PRODUCT_NAME)_$(HW_PLATFORM).elf \
	$(TARGET_FOLDER)/$(PRODUCT_NAME)_$(HW_PLATFORM).hex \
	$(TARGET_FOLDER)/$(PRODUCT_NAME)_$(HW_PLATFORM).bin \
	$(TARGET_FOLDER)/$(PRODUCT_NAME)_$(HW_PLATFORM).eep \
	$(TARGET_FOLDER)/$(PRODUCT_NAME)_$(HW_PLATFORM).lss \
	$(TARGET_FOLDER)/$(PRODUCT_NAME)_$(HW_PLATFORM).srec

DEBUG_FLAGS = -DDEBUG -O0 -g3


all: $(TARGETS) show_size

include $(TOP_LEVEL_DIR)/src/xt_xep.make
